import * as React from "react";
//COMPONENTS
import { Layout } from "../components";

const Home = () => (
    <Layout>
        <main>
            <h1>Hey, Moon!</h1>
        </main>
    </Layout>
);

export default Home;
